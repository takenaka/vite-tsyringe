<script setup lang="ts">
import { inject } from 'vue';
import HelloWorld from './components/HelloWorld.vue';
import { greeterSymbol, type IGreeter } from './package/greeter';

const greeter = inject<IGreeter>(greeterSymbol);
</script>

<template>
  <header>
    <img
      alt="Vue logo"
      class="logo"
      src="./assets/logo.svg"
      width="125"
      height="125"
    />

    <div class="wrapper">
      <HelloWorld :msg="greeter?.greet() || ''" />
    </div>
  </header>
</template>

<style scoped>
header {
  line-height: 1.5;
}

.logo {
  display: block;
  margin: 0 auto 2rem;
}

@media (min-width: 1024px) {
  .logo {
    margin: 0 2rem 0 0;
  }

  header .wrapper {
    display: flex;
    place-items: flex-start;
    flex-wrap: wrap;
  }
}
</style>
